{
  "name": "@surfingdog/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "migrations": "drizzle-kit generate && node scripts/embed-migrations.mjs"
  },
  "dependencies": {
    "@surfingdog/spec": "workspace:*",
    "zod": "catalog:",
    "drizzle-orm": "catalog:",
    "@surfingdog/platform": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "drizzle-kit": "^0.31.11",
    "@types/node": "catalog:"
  }
}
