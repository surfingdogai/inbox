{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "POST /v1/contests/{id}/response",
  "type": "object",
  "properties": {
    "answer": {
      "type": "string",
      "enum": ["withdraw", "dispute"],
      "description": "withdraw takes the outcome back, at any time; dispute says it is right, while the contest is open and until its respond_by (else 422 contest_window). ADR-017 A2.9."
    }
  },
  "required": ["answer"]
}
