{
  "files": {
    "receipts.json": "Receipt and acknowledgement vectors (ADR-016): fixed issuer and agent keys, the subject pseudonym derivation, two signed receipts that any Ed25519 implementation must reproduce byte for byte, one valid acknowledgement, and the refusals with the error code each must produce. Generated by packages/core/scripts/gen-receipt-vectors.ts; checked on Node and workerd by packages/core/test/receipts-vectors.test.ts.",
    "receipts-v2.json": "Receipt claims v2 (ADR-017 §3.2): a promise and its outcome for each of the eleven outcomes an inbox records, an acknowledgement naming a pass reference (pas), refused claims with the code a network answers, the outcome table of §3, and every path through the booking and order state machines with the outcome it records (§3.1). Generated by packages/core/scripts/gen-network-vectors.ts; checked by packages/core/test/network-vectors.test.ts.",
    "signatures.json": "Signed requests (ADR-017 §2.4, §7.2): sdi-instance/1, sdi-agent/1 with a self-held key and with Web Bot Auth, forwarded signatures (agent_key), each with its signature base, and refusals with their codes. Generated by packages/core/scripts/gen-network-vectors.ts; checked by packages/core/test/network-vectors.test.ts.",
    "passes.json": "The strings a person holds (key, pass, pass reference, session), ppid, email_mac, secret hashes and email normalisation (ADR-017 §2). Generated by packages/core/scripts/gen-network-vectors.ts; checked by packages/core/test/network-vectors.test.ts.",
    "scoring.json": "Scores (ADR-017 §5), from the network: every worked example with its scoring_time, the R5 grid, the ageing word cap, hold and release, R32.",
    "ordering.json": "Order (ADR-017 §6), from the network: rank_shuffle values above 2^53, one snapshot exercising every ORDER BY clause, and cursors."
  },
  "coming": "Review vectors arrive with the review format."
}
